export type ShoppingItem = {
  id: string;
  listName: string;
  title: string;
  quantity: string;
  department: string;
  estimatedPrice: number;
  buyer: string;
  notes: string;
  purchased: boolean;
};
